{
  "name": "liveyaku-extension",
  "version": "0.1.0",
  "private": true,
  "description": "LiveYaku browser extension (MV3). Dashboard/settings/glossary UI (blueprint §2.1 C1-C4) that talks to the local helper via Native Messaging.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-static.mjs",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
